schema-version = "1.10.0"

[install]

[vars]

[hook]
on-activate = '''
  # Symlink repos from RogerNavelsaker based on pattern
  for r in ../nix-repos nix-config nix-lib nix-secrets nix-keys; do
    [ -d "$r" ] && ln -sf "$r" .
  done
'''
